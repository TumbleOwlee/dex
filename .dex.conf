[project]
profile = format

[format]
image = alpine:latest

[alias:add-shfmt]
desc = Install shfmt.
cmd = apk add --no-cache shfmt
hidden = true

[alias:ex-shfmt]
desc = Run shfmt.
cmd = shfmt -w -i 4 -ln bash -ci -kp dex
hidden = true

[alias:fmt]
desc = Format all shell scripts.
pipeline = add-shfmt,ex-shfmt

[alias:install-deps]
desc = Install dependencies.
cmd = apk add git bash
interactive = false
hidden = true

[alias:safe-git]
desc = Set /workspace as safe directory.
cmd = git config --global --add safe.directory /workspace
interactive = false
hidden = true

[alias:bash]
desc = Run bash.
cmd = /bin/bash
interactive = true
hidden = true

[alias:shell]
desc = Run shell.
pipeline = install-deps,safe-git,bash

[copilot]
image = debian:trixie-slim

[alias:add-deps]
desc = Install dependencies.
cmd = apt-get update && apt-get install -y curl git && apt-get purge && apt-get autoremove && apt-get autoclean && apt-get clean
hidden = true

[alias:add-copilot]
desc = Install dependencies.
cmd = curl -fsSL https://gh.io/copilot-install | bash
hidden = true

[alias:run-copilot]
desc = Run copilot.
cmd = copilot
env = COPILOT_GITHUB_TOKEN
interactive = true
hidden = true

[alias:run]
desc = Run copilot.
pipeline = add-deps,add-copilot,run-copilot
